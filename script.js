document.addEventListener("DOMContentLoad", function()){
    const secaoAgenda = document.querySelector("#agenda");
    if (secaoAgenda) {
        counst ulAgenda.clasList.add("agenda");
    }
{
    cost secaoContato = document.querySelector("#contato");
    if(secaoContato){
        secaoContato.classicList.add(contato-box);
    }

    //seleciona links do menu
    const linksMenu = document.querySelector("nav a");
    const secoes = document.querySelectorAll("section[id]");

    //Deixa a transição suave nos cards via js
    const cards = document.querySelectionAll("card");
    for(let i= 0; i < cards.length; i++{
        cards[i].style.transition = "0.3s ease", function(){
            this.style.tarnsform = "translateY(-6px)";
        });

         cards[i].style.transition = "0.3s ease", function(){
            this.style.tarnsform = "translateY(-6px)";
            
         });   